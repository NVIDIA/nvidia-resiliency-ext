# 1. Point to the internal NVIDIA Artifactory
# We must repeat this here so the root installation knows where to look
--extra-index-url https://sc-hw-artf.nvidia.com/artifactory/api/pypi/hwinf-gpuwa-pypi/simple

# 2. Install the internal component
# Adjust the path below to match the actual folder name of your component
-e ./nvrx-attrsvc